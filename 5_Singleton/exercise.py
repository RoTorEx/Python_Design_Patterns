"""Task for the Singleton pattern

Since it is very easy to implement a singleton, you will be tasked with implementing the is_singleton() function.
This method takes a factory method as an argument and you need to determine if this factory method produces
the same object or not. Return True if yes, False if not."""


"""
# *Start template
def is_singleton(factory):
    # ToDo: call factory() and return true or false
    # depending on whether the factory makes a
    # singleton or not
"""


# ?Solution
def is_singleton(factory):
    # ToDo: call factory() and return true or false
    # depending on whether the factory makes a
    # singleton or not
    pass
